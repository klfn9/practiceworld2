<h2>投稿フォーム</h2>
  <% form_with model: @trip do |f| %>
    <h4>画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>タイトル</h4>
      <% f.text_field :title %>
    <h4>説明</h4>
      <% f.text_field :body %>